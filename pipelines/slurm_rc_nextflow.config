#!/usr/bin/env nextflow

process {
    executor = 'slurm'
    perJobMemLimit = true
    withLabel: rc_default {
        queue = '1c2m20d'
        clusterOptions = ' --mem 2000 '
    }
    withLabel: rc_64Gb_32C {
        queue = '32c128m60d'
        clusterOptions = ' -c 32 --mem 64000 '
    }
}
