#!/usr/bin/env nextflow

params {
    ensembl_root_dir = "$ENSEMBL_ROOT_DIR",
    storage_dir = "/hps/nobackup/flicek/ensembl/compara",
    warehouse_dir = "/nfs/production/flicek/ensembl/compara/warehouse",
}
